{
  "name": "@kais/sdk",
  "version": "0.1.0",
  "description": "kAIs SDK — connect your apps to kAIs cells via NATS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "nats": "^2.28.0"
  },
  "devDependencies": {
    "typescript": "^5.5",
    "vitest": "^2.0"
  }
}
